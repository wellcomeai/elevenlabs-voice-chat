# ElevenLabs Conversational AI - Render.com Dependencies
# Версия БЕЗ PyAudio для облачного деплоя

# Основные зависимости
aiohttp==3.9.1              # HTTP клиент для API запросов
websockets==12.0            # WebSocket клиент для ElevenLabs
numpy==1.24.3              # Численные вычисления

# Утилиты
colorlog==6.7.0            # Цветное логирование
python-dotenv==1.0.0       # Загрузка переменных из .env

# Веб сервер для health checks
fastapi==0.104.1           # REST API фреймворк
uvicorn==0.24.0            # ASGI сервер
gunicorn==21.2.0           # WSGI сервер

# Дополнительные пакеты
pydantic==2.5.0            # Валидация данных
uvloop==0.19.0             # Быстрый event loop (Linux)

# ПРИМЕЧАНИЕ: PyAudio исключен для облачного деплоя
# В облачной среде нет доступа к микрофону/динамикам
# Используйте локальную версию с PyAudio для полного функционала
